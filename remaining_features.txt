features to design:

-form component for 
				-- allowing an invoice edit to be saved as a draft --- do i need this ?
	++ validation and error display
	** challengingParts: calendar

- confirm deletion modal
- data display of the numbers (and handling if totals cannot be calculated in case the thing is a draft). There is also a bit of a mess with the use of toFixed. 

- dark theme (update colors to be variables)

- tablet design
- mobile desing